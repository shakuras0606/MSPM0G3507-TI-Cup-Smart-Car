# MSPM0G3507 驱动 ST7735

## 接线

| ST7735 | MSPM0G3507 | 说明 |
|---|---:|---|
| VCC | 3.3 V | 使用 3.3 V 逻辑 |
| GND | GND | 必须共地 |
| SCL/SCK | PB9 | 软件 SPI 时钟 |
| SDA/MOSI | PB8 | 软件 SPI 数据 |
| CS | PB14 | 低有效 |
| DC/A0 | PB11 | 命令/数据选择 |
| RST/RES | PB10 | 低有效复位 |
| BL/LED | PB26 | 背光控制 |

裸屏如果没有背光限流电阻，BL 不应直接由 GPIO 承担背光电流。

## 右边和下边彩线的原因与修复

ST7735S 手册定义 `CASET(0x2A)`、`RASET(0x2B)` 和 `RAMWR(0x2C)` 控制
GRAM 写入窗口。控制器内部最大地址区域可到 132×162，但本模块玻璃可视区
为 128×160，二者原点不一定重合。

这块实物同时在最右列和最下行出现一条旧颜色，说明写入窗口相对可视区域
向左、向上各错了 1 像素。修复为：

```c
#define ST7735_X_OFFSET  1u
#define ST7735_Y_OFFSET  1u
```

修复后全屏逻辑窗口 `(0,0)..(127,159)` 映射为控制器窗口
`(1,1)..(128,160)`，最后一列和最后一行会被真正覆盖。

## 刷新方案

- 初始化时只全屏清零一次；
- 固定标签只绘制一次；
- `ScreenTask_Update()` 每 200 ms 只刷新数值矩形；
- 字符改为一次窗口、连续像素写入；
- 软件 SPI 不关闭中断，所以编码器采样和 UART DMA 可并行继续。

如更换不同厂家屏幕后偏移不同，只调整 `ST7735_X_OFFSET` 和
`ST7735_Y_OFFSET`。红蓝互换时检查 `MADCTL_BGR`；白屏时检查供电、共地、
RST、CS、SCLK/MOSI 波形和实际控制器型号。

手册：<https://www.displayfuture.com/Display/datasheet/controller/ST7735S.pdf>
