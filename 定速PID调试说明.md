# 双轮定速PID调试说明

## 1. 控制结构

两个车轮分别使用独立控制器：

```text
共同目标RPM ─┬─ M1 PID ─ DRV8871 M1 ─ M1车轮 ─ M1编码器 ─┐
             └─ M2 PID ─ DRV8871 M2 ─ M2车轮 ─ M2编码器 ─┘
```

控制周期为10 ms，编码器每10 ms采集累计计数，使用最近100 ms滑动窗口
计算车轮RPM。PID初调采用PI，微分项暂时为0。

## 2. DRV8871工作方式

TI数据手册真值表：

| IN1 | IN2 | 状态 |
|---:|---:|---|
| 0 | 0 | High-Z滑行，约1 ms后睡眠 |
| 0 | 1 | 反转 |
| 1 | 0 | 正转 |
| 1 | 1 | 低侧制动、慢衰减 |

20 kHz正转PWM在`10`驱动和`11`制动之间切换，符合TI推荐方式。

软件限制不能替代硬件限流。必须核对每块DRV8871的ILIM电阻：

```text
ITRIP(A) ≈ 64 / RILIM(kΩ)
```

例如32 kΩ约为2 A；数据手册要求RILIM不得低于15 kΩ。VM旁需要0.1 µF
陶瓷旁路和足够的储能电容，功率地、逻辑地与MCU必须可靠共地。

## 3. 板载PB21操作

核心板板载PB21低有效，使用内部上拉和30 ms软件消抖。每次按下切换：

```text
0 → 50 → 100 → 150 → 200 → 250 → 300 → 0 RPM
```

屏幕`SET`显示目标，`RPM1/RPM2`显示实际值。

## 4. VOFA通道

选择JustFloat、921600 baud、11个通道：

| 通道 | 名称 |
|---:|---|
| 0 | TARGET_RPM |
| 1 | M1_RPM |
| 2 | M2_RPM |
| 3 | M1_OUT |
| 4 | M2_OUT |
| 5 | M1_ERROR |
| 6 | M2_ERROR |
| 7 | DIRECTION_FAULT_MASK：0=正常，1=M1反向，2=M2反向，3=两路反向 |
| 8 | YAW |
| 9 | PITCH |
| 10 | ROLL |

## 5. 首次上电顺序

1. 把车轮架空，电机电源先限流。
2. 下载固件，确认目标为0且电机不转。
3. 按一次板载PB21，目标变为50 RPM。
4. 两个实际RPM都必须为正。
5. 启动后有300 ms宽限；此后任一轮连续5次为负，软件才触发方向保护。
6. 查看VOFA通道7：`1`表示M1方向错误，`2`表示M2方向错误，`3`表示两路。
7. 根据实物修正`CONFIG_SPEED_Mx_COMMAND_SIGN`或编码器方向宏。
8. 方向正确后才逐步测试100、150 RPM。

## 6. PI调参顺序

参数位于`inc/project_config.h`：

```c
CONFIG_SPEED_M1_KP / KI / KD
CONFIG_SPEED_M2_KP / KI / KD
```

推荐步骤：

1. 先令`Ki=0、Kd=0`，从小到大增加Kp。
2. 让实际RPM能快速接近目标，但不要持续振荡。
3. 将Kp降到刚才振荡临界值的约50%～70%。
4. 从小到大增加Ki，直到静差能在可接受时间内消除。
5. 当前编码器分辨率和100 ms窗口下先保持`Kd=0`。
6. 两个电机差异较大时分别调整M1和M2参数，不要强求参数相同。

常见现象：

| 波形 | 调整 |
|---|---|
| 上升很慢、输出不高 | 增大Kp |
| 长期达不到目标 | 增大Ki |
| 快速来回振荡 | 减小Kp或Ki |
| 大幅超调后慢慢回来 | 减小Ki及积分上限 |
| 输出长期等于900 | 目标过高、电压不足、机械卡滞或负载过大 |
| RPM呈小台阶但总体稳定 | 编码器量化，属正常现象 |

## 7. 初始参数

当前保守初值：

```text
M1/M2 Kp = 2.0 permille/RPM
M1/M2 Ki = 8.0 permille/(RPM·s)
M1/M2 Kd = 0
输出范围 = 0..900 permille
积分范围 = 0..800 permille
```

这些参数只是安全起点，最终值取决于电池电压、电机、轮径、车重和机械阻力。

数据手册：<https://www.ti.com/lit/ds/symlink/drv8871.pdf>
