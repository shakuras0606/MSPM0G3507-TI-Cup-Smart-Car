# 永久在线 Yaw 串级控制调试说明

## 这不是一次性的90度定位

当前控制器是始终运行的全局航向环：

```text
全局Yaw参考角 - WT61实际Yaw
              ↓
      角度PI + Z轴角速度阻尼
              ↓ turn_rpm
 M1目标=+turn_rpm，M2目标=-turn_rpm
              ↓
        双轮前馈+PI速度内环
              ↓
           DRV8871
```

首次收到有效WT61角度时，将该角度定义为相对`0°`。从此以后，无论是否
按过B21、是否已经到达目标、误差是小还是大，Yaw外环都每10 ms计算一次。

`completed`和VOFA状态`2`只表示当前曲线处于锁定范围，不会停止算法。

## 连续控制律

```text
turn_rpm = Kp * yaw_error
         + Ki * integral(yaw_error)
         - Kd * gyro_z_dps
```

- P项决定角度偏差产生多大的差速目标。
- I项只在小误差区工作，用于克服左右轮不一致、地面阻力造成的固定偏差。
- D项直接使用WT61的Z轴角速度，不对Yaw角度做数值差分，因此不会在
  `±180°`换向或角度量化时产生微分尖峰。
- 输出限幅只限制最大轮速，不会因误差过大退出控制。
- 已删除Yaw误差发散停机和到位后退出逻辑。
- 仍保留WT61离线和电机长时间堵转保护。
- 编码器方向检查已经完成标定，当前用
  `CONFIG_SPEED_DIRECTION_FAULT_ENABLE=0`关闭，避免人工反拖车轮时误停；
  更换电机、编码器或极性配置后，应架空车辆临时设为`1`重新验证。

## 当前参数

参数均位于`inc/project_config.h`：

```text
速度M1：Kp = 3.60，Ki = 4.00，Kd = 0
速度M2：Kp = 3.60，Ki = 4.00，Kd = 0
编码器测速窗口 = 50 ms
启动托底 = 160‰，实速达到目标60%后退出

Kp = 1.35 RPM/deg
Ki = 0.02 RPM/(deg*s)
Kd = 0.10 RPM/(deg/s)
WT61 gyro_z极性 = -1
最大差速目标 = ±90 RPM
积分范围 = ±6 RPM
积分分离范围 = ±5°
浮点命令滤除 = 0.20 RPM
静摩擦补偿触发误差 = 1.50°
静摩擦补偿触发角速度 = 8°/s
静摩擦补偿目标 = 3 RPM
VOFA锁定显示范围 = ±0.80°
```

`0.20 RPM`仅滤除最终浮点命令的末位噪声，角度PI和积分仍在每周期运行，
它不是“误差超过某个范围才启动”的状态机。

最新一轮数据中，90度误差降到10度约需`1.21~1.43 s`，但稳定进入
`±1度`约需`2.1~3.6 s`。速度环仍约有`50 ms`滞后，大误差阶段平均
目标为`55.5 RPM`、实际约`43.5 RPM`，所以速度Kp由3.0提高到3.6。

尾段小于2度时，固定3 RPM补偿平均每秒换向约15次。为避免车轮持续追赶
上一方向，固定补偿的角度触发值由0.8度提高到1.5度、角速度触发值由
5度/秒提高到8度/秒；角度PI仍持续计算，不会退出全局Yaw跟踪。

数据还确认`d(Yaw)/dt`和WT61 `gyro_z`的相关系数为`-0.945`，两者符号
相反。现在先用`CONFIG_YAW_GYRO_SIGN=-1`统一方向，再施加负反馈阻尼。
旧代码的阻尼符号实际为正反馈，是持续摆动的重要原因。

## B21和巡线接口

- 上电、不按B21：始终保持相对`0°`。
- B21每按一次：长期目标增加`90°`。
- 运动中按B21：继续增加目标，不会取消航向环。
- 后续巡线模块应调用：

```c
YawControl_SetTargetYaw(target_yaw_deg, BSP_Time_Millis());
```

巡线外层只更新“希望车头朝向哪里”，Yaw环负责追踪该参考角，速度内环
负责让左右轮达到对应差速目标。不要让巡线PID和Yaw PID同时直接写电机PWM。

## VOFA 10通道

| 通道 | 数据 |
|---:|---|
| 0 | M1_TARGET_RPM |
| 1 | M1_ACTUAL_RPM |
| 2 | M2_TARGET_RPM |
| 3 | M2_ACTUAL_RPM |
| 4 | YAW_TARGET |
| 5 | YAW_ACTUAL |
| 6 | YAW_ERROR |
| 7 | YAW_TURN_RPM |
| 8 | GYRO_Z_DPS |
| 9 | YAW_STATE |

```text
 0 = 等待WT61首次有效角度
 1 = 永久在线控制器正在输出纠偏
 2 = 当前锁定；控制器仍在计算
-1 = WT61离线
-2 = 速度内环方向错误或堵转保护
```

## 抗干扰测试顺序

1. 车轮落地，上电后不要按B21，确认目标和实际Yaw都从`0°`开始。
2. 用手快速扭动车体约`10°`后立即松手。误差可以突然增大，但状态不能
   因角度幅度变成负数；turn_rpm应立即反向并持续回到0°。
3. 分别测试`5°、15°、30°`扰动，记录峰值误差、回到`±1°`时间和过冲。
4. 若回正整体偏慢且没有明显过冲，先将Yaw Kp从`1.35`增加到`1.45`。
5. 若回正快但越过0°后摆动，先将Kd从`0.10`增加到`0.12`。
6. 若最终长期偏在同一侧，再将Ki从`0.02`增加到`0.03`；若低频摆动则
   减小Ki，不要同时修改Kp。
7. 若小扰动完全推不动电机，把静摩擦补偿从`3 RPM`逐步提高；若零点
   附近敲击明显，则降低补偿或提高触发误差，但不要用“动作完成”停环。

调参顺序始终是：双轮速度环 → Yaw角速度阻尼/Kp → 小误差Ki → 巡线外层。
